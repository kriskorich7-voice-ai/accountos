# Deepgram API key — enables voice input (STT) and spoken responses (TTS) in the Copilot.
VITE_DEEPGRAM_API_KEY=

# Anthropic API key — read server-side by /api/anthropic (Vercel serverless function).
# Never expose this to the client; it is not prefixed with VITE_.
ANTHROPIC_API_KEY=
